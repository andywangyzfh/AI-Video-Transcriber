# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom OpenAI Base URL (for proxy or alternative endpoints)
OPENAI_BASE_URL=https://api.openai.com/v1

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000

# Whisper Model Configuration (Optional)
# Options: tiny, base, small, medium, large
WHISPER_MODEL_SIZE=base

# YouTube Authentication (Optional)
# For downloading members-only or private videos
# Set to the browser you're logged into YouTube with
# Options: chrome, firefox, safari, edge, opera, brave, none
# Default: chrome
YTDLP_BROWSER=chrome

# Alternative: Use cookies file instead of browser
# Export cookies to Netscape format using a browser extension
# YTDLP_COOKIES_FILE=/path/to/cookies.txt
